name: website

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
      -
        name: Gen build file
        run: |
          cd ./my-portfolio
          yarn build
      -
        name: Deploy to GitHub Pages
        if: success()
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: gh-pages
          build_dir: my-portfolio/build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}